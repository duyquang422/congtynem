<?php
	$name 		= $this->Item['name'];	
	$parent 	= $this->Item['parent_name'];
	if(empty($parent)){
		$parent = "Not parent";
	}else{
		$parent 	= $this->Item['parent_name'];
	}
	$status			= $this->Item['status'];
	if($status == 1){												
		$status = $this->cmsIconButton('Active', $this->imgUrl . '/icon/active.png') . " Active";
	}else{
		$status = $this->cmsIconButton('Inactive', $this->imgUrl . '/icon/inactive.png') . " Inactive";
	}
	
	$order 		= $this->Item['order'];	
	$inputInfo 		= array('name'=>'picture');
	$urlimage   = FILES_URL . '/category/images100x100/'. $this->Item['picture'];
	$picture  = '<img src="' . $urlimage . '" width="" height="40">'; 		
	$Input 		= array(
						array('Label'=>'Category Name','input'=>$name,'info'=>1),
						array('Label'=>'Picture','input'=>$picture,'info'=>1),
						array('Label'=>'Parent','input'=>$parent,'info'=>1),
						array('Label'=>'Status','input'=>$status,'info'=>1),
						array('Label'=>'Ordering','input'=>$order,'info'=>1),
						);
	if(count($this->errors)>0){
		$errors = $this->formErrors($this->errors,array('id'=>'errors'));
	}
?>
<form name="appForm" method="post" action="" enctype="multipart/form-data">

                        <?php echo $this->render('toolbar.php')?>
                        <div class="clr"></div>
                        <?php echo $this->render('sublink.php')?>
                        <?php echo $errors;?>
                        <!-- BEGIN: CONTENT -->
                        <div id="element-box">
                            <div class="t">
                                <div class="t">
                                    <div class="t"></div>
                                </div>
                            </div>
                            <div class="m">
                                <!-- BEGIN: ELEMENT BOX -->
						<div id="adminfieldset">
                             <div class="adminheader">Detail</div>								
								<?php echo $this->partialLoop('rows.php',$Input) .$current_stadium ;?>                                
							</div>
							
                                <!-- END: ELEMENT BOX -->	
                              <div class="clr"></div>
                            </div>
                            <div class="b">
                                <div class="b">
                                    <div class="b"></div>
                                </div>
                            </div>
                        </div>
                        <!-- END: 	CONTENT -->
                    </form>